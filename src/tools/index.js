import { tool as pencil } from "./pencil";
import { tool as eraser } from "./eraser";
import { tool as select } from "./select";
import { tool as brush } from "./brush";

export default {
  pencil,
  eraser,
  select,
  brush
};
